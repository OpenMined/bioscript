# VNtyper BioScript Tests

This directory will contain BioScript-owned parity tests for the VNtyper port.
Use upstream VNtyper tests as the reference, but keep these tests runnable
without requiring the entire upstream Python environment unless explicitly
marked as integration tests.

